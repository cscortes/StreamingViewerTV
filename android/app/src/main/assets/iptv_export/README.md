# Place viewer.db here before assembling the APK.
#
# From the repo root:
#   make android-sync-db
#
# Or manually:
#   cp iptv_export/viewer.db android/app/src/main/assets/iptv_export/viewer.db
#
# Build the catalog first with: make build   or   make build-probed
